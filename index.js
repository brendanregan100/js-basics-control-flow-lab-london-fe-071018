// Write your code in this file!
let distance = 400;

function scuberGreetingForFeet(){
  if (distance <= 400){
    return ("This one is on me!");
  }
  else if (distance > 400){
    return ("We have exceeded free sample distance - you must pay now");
  }
}